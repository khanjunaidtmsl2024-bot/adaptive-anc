# Minimal Embedded Deployment Dependencies for Raspberry Pi 4 / 5
# PS 26052 — Defence-Grade Adaptive ANC
# No heavy deep-learning frameworks (PyTorch) required on the edge device!

numpy>=1.24.0
onnxruntime>=1.15.0
soundfile>=0.12.0

# Hardware ALSA Audio I/O (Install on Linux Pi):
# Requires system dependency: sudo apt-get install -y libasound2-dev
pyalsaaudio>=0.9.2

# Optional JIT Acceleration (Pure NumPy fallback is included):
numba>=0.58.0
