# PS 26052 — Adaptive Defence ANC Dependencies
# Install: pip install -r requirements.txt

# --- Core Mathematics & Signal Processing ---
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0

# --- Standardized Objective Speech Evaluation ---
pystoi>=0.3.0
pesq>=0.0.4

# --- Configuration & Testing ---
pyyaml>=6.0
pytest>=7.4.0

# --- Performance Accelerator (JIT DSP hot path) ---
# 0.63.x caps NumPy at <2.3; 0.67.0 supports NumPy 2.4 (verified on 2.4.6).
# Without a compatible numba the fast backends silently fall back to the
# pure-Python reference implementations.
numba>=0.67.0

# --- Visualization & Diagnostics Export ---
matplotlib>=3.7.0

# --- Optional Neural / Live Audio ---
# torch>=2.0.0
# sounddevice>=0.4.6
# librosa>=0.10.0

